machine:
  environment:
    ANDROID_HOME: /usr/local/android-sdk-linux

dependencies:
  pre:
    - chmod +x gradlew
    - echo y | android update sdk --no-ui --all --filter "android-17,build-tools-21.1.2"